 <div class="control">
            {{#each stories}}
            <field class="textarea has-fixed-size" rows="1">
              {{title}} by {{user.username}} on {{format_date date_created}}
            </field>

            <textarea class="textarea" rows="3" id="blogcontent">
              {{content}}
            </textarea>
          </div>
