{{!-- displays current blogs --}}

<div class="card">
      <div class="card-header" id="cardTitle">
        {{title}}
      </div>
      <div class="card-body">
        <p class="card-text">{{content}}</p>
      </div>
    </div>